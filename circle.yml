machine:
  java:
      version: openjdk7
deployment:
  production:
      branch: master
      commands:
        - chmod u+x gradlew
        - ./gradlew desktop:gaiaskyJar


dependencies:
  override:
    - ANDROID_HOME=/usr/local/android-sdk-linux ./gradlew dependencies
